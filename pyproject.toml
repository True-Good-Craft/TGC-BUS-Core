[build-system]
requires = ["setuptools>=65", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tgc-bus-core"
version = "0.11.0"
description = "Packaging stub for TGC BUS Core."
readme = "README.md"
requires-python = ">=3.11"
